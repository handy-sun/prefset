const cppQuestions_Category1 = {
  "cplusplus_core": [
    // Task 1.1: C++11~20新特性、基础理论与语法栈 (1-25)
    {
      q: "C++ 与 C 语言的最显著区别是什么？",
      a: "C 是面向过程的语言；C++ 支持面向对象编程(OOP)和泛型编程，具有封装、继承、多态等特性，并提供丰富的 STL 库。",
      d: "主要区别：<br>1. 编程思想：C 面向过程，C++ 支持多范式（面向过程、OOP、泛型编程）。<br>2. 核心特性：C++ 引入了类、虚函数、模板、运算符重载等。<br>3. 内存管理：C++ 有 <code>new/delete</code> 和 RAII（智能指针），C 是 <code>malloc/free</code>。<br>4. 类型安全：C++ 更加严格（如强类型枚举、<code>static_cast</code> 等类型转换）。",
      s: "C++ 是一门支持多范式的高级语言，向下兼容 C。",
      lv: "basic"
    },
    {
      q: "什么是 C++11 的 auto 和 decltype？",
      a: "auto 用于在编译期推断变量类型；decltype 用于在编译期获取表达式的具体类型，常用于泛型编程。",
      d: "<code>auto x = 5;</code>（x被推导为int）。<br><code>decltype(expr)</code> 可以推断出 expr 的类型而不实际计算它。<br>注意：auto 通常会抛弃引用属性和顶层 const（例如推导 <code>const int&</code> 可能变为 <code>int</code>），而 decltype 则会严格保留类型的全部特征（包括引用和 const）。",
      s: "auto 关注“值推导”，decltype 关注“精准类型提取”。",
      lv: "basic"
    },
    {
      q: "C++11 为什么引入 nullptr，相比 NULL 的优势是什么？",
      a: "nullptr 是具有自身类型 (std::nullptr_t) 的严格空指针常量，可以避免在函数重载时被误解析为整型 0。",
      d: "NULL 在 C++ 中通常被宏定义为 0。当发生函数重载判断（例如 <code>void func(int)</code> 和 <code>void func(char*)</code>）时，传入 NULL 会调用 <code>func(int)</code> 产生歧义。nullptr 的类型是 <code>std::nullptr_t</code>，可以隐式转换为任何指针类型，但不转换为整型，保证了严格的类型安全。",
      s: "现代 C++ 永远应该使用 nullptr 替代 NULL。",
      lv: "basic"
    },
    {
      q: "什么是 C++11 的 Range-based for (基于范围的for循环)？",
      a: "一种语法糖，用来更加安全、简洁地遍历容器或数组的每个元素，避免了传统的以索引或迭代器为主的繁琐遍历方式。",
      d: "语法：<code>for (auto& item : container) { ... }</code>。<br>编译器会将其展开为使用 <code>begin()</code> 和 <code>end()</code> （或数组地址）构建迭代器的循环。<br>建议：读取使用 <code>const auto&</code>，修改使用 <code>auto&</code>，传递拷贝使用 <code>auto</code>。",
      s: "极大简化了遍历操作，提升代码可读性并降低越界风险。",
      lv: "basic"
    },
    {
      q: "C++11 中 const 和 constexpr 的区别？",
      a: "const 表示变量“只读”，在运行期确保其不被修改；constexpr 严格表示“常量表达式”，要求变量或函数必须在编译期出结果。",
      d: "<code>const int x = getX();</code> 可以在运行时赋值。<br><code>constexpr int y = 5;</code> 必须在编译期间就能得出值。<br>如果把函数声明为 constexpr，只要传入的参数是编译期常量，其计算结果就能在编译期计算完成并直接替换（常用于取代宏定义硬编码值），进而提升运行性能。",
      s: "const 是“运行时不可修饰的承诺”，constexpr 是“编译期可计算的断言”。",
      lv: "intermediate"
    },
    {
      q: "静态断言 (static_assert) 的作用是什么？它与 assert 的区别？",
      a: "static_assert 用于在编译期进行条件检查。如果条件事假，则导致编译失败。assert 是在运行期起作用的。",
      d: "语法：<code>static_assert(sizeof(void*) == 8, \"Require 64-bit platform\");</code><br>由于 static_assert 发生在编译期间，它对运行时性能没有任何开销，这在开发涉及架构依赖的底层系统或基于模板的泛型代码时特别有用。",
      s: "错误尽早暴露的典范：将运行时报错提前到编译期拦截。",
      lv: "intermediate"
    },
    {
      q: "强类型枚举 (enum class) 相比传统 enum 解决了哪些痛点？",
      a: "传统 enum 的作用域会泄露到外部，且会隐式转换为整型。enum class 具备独立作用域，强类型检查。",
      d: "传统枚举：<code>enum Color { RED, BLUE }; int x = RED;</code>（直接全局可见，可能造成命名冲突，且自动转 int）。<br>强类型枚举：<code>enum class Color { RED, BLUE };</code>。使用时必须带前缀 <code>Color::RED</code>，而且不能隐式跟整型发生转换（必须使用 <code>static_cast&lt;int&gt;(Color::RED)</code>），底层基础类型也可以显式指定（如 <code>enum class Color : uint8_t { ... }</code>）。",
      s: "使用 enum class 可以避免命名污染并提升类型安全性。",
      lv: "basic"
    },
    {
      q: "C++11 中 override 和 final 关键字的作用？",
      a: "override 强制编译器检查该虚函数是否成功重写了基类的虚函数；final 用于禁止类被继承或虚函数被进一步重写。",
      d: "如果在派生类写错重写函数签名，没有 <code>override</code> 时编译器会创建新的函数；使用 <code>override</code> 则会立即报错。<br><code>final</code>：修饰类 <code>class A final {};</code> 或修饰虚函数。阻止派生类进行扩展。这在提供不可覆写的公共API库中非常常见。",
      s: "增强了面向对象设计的确定性与安全性。",
      lv: "basic"
    },
    {
      q: "说说 C++11 的 =default 和 =delete？",
      a: "=default 请求编译器主动生成默认的特殊成员函数（如构造/析构），=delete 明确禁止编译器生成指定的函数（也可用于禁用重载）。",
      d: "为了防止资源泄漏或多重析构，你常常写了自定义的复制构造函数，这使得编译器不再默认生成无参构造。通过 <code>类的名字() = default;</code> 可恢复生成。<code>=delete</code> 常用于单例模式中，通过 <code>Singleton(const Singleton&) = delete;</code> 来明确禁止对象的拷贝。",
      s: "精准控制类的构造行为和拷贝语义，比早期把函数声明为 private 的手法更优雅。",
      lv: "intermediate"
    },
    {
      q: "简述 C++11 左值引用和右值引用？什么场景会用到右值引用？",
      a: "左值引用绑定到有名字的持久对象；右值引用（&&）绑定到即将销毁的临时对象（将亡值）。用于支持移动语义和完美转发。",
      d: "传统的拷贝需要在内存中进行深拷贝，非常低效。右值引用配合移动构造函数，可以直接“窃取”临时对象的内存指针并将其置空，这就是“移动语义”。从而在函数返回值较大或利用 <code>std::vector::push_back</code> 传入临时对象时，大幅减少开销。",
      s: "右值引用是 C++ 单次跨越式性能提升的核心底层基石。",
      lv: "advanced"
    },
    {
      q: "std::move 和 std::forward 的区别？",
      a: "std::move 无条件地将参数转换为右值引用；std::forward 是完美转发，仅当被转发对象原本是个右值时，才将其转为右值引用。",
      d: "<code>std::move(x)</code> 内核是不移动任何数据的强制类型转换 <code>static_cast&lt;T&&&gt;(x)</code>，通常用于提示编译器调用移动构造。<br>由于传参机制，即便函数接收到的是右值引用（比如通过万能引用模板 <code>T&&</code>），在这个函数内部它拥有了名字，又变成了左值。为把其原汁原味的左右属性传递给下一级调用的函数，必须包一层 <code>std::forward&lt;T&gt;(x)</code>。",
      s: "move 是强势剥夺（转右），forward 是完美继承（保留左右属性）。",
      lv: "advanced"
    },
    {
      q: "简述 C++ Lambda 表达式的基础语法与底层实现？",
      a: "语法 `[捕获列表](参数列表) -> 返回值类型 { 函数体 }`。底层由编译器实现为一个带有 operator() 的匿名函数对象（仿函数 / 闭包类）实例。",
      d: "捕获列表支持 `[=]`按值，`[&]`按引用。`[this]`按值捕获当前类指针等。<br>每定义一个 lambda，编译器都会为其生成一个独一无二的类。它的生命周期符合普通对象的作用域，所以在函数回调与 STL 算法函数（如 `std::sort`）结合时非常方便。",
      s: "提供了一种便捷定义内联、匿名回调逻辑的方式。",
      lv: "intermediate"
    },
    {
      q: "Lambda 表达式按值捕获和按引用捕获有什么风险？什么时候会出现悬空引用？",
      a: "按引用捕获 [&] 若生命周期管理不当，极易导致悬挂引用（Dangling Reference）；按值捕获可能拷贝大对象影响性能，捕获隐式指针依然可能无效。",
      d: "当使用 [&] 捕获局部变量，并将 Lambda 返回至外部保存或交给异步线程执行时，外部函数执行完毕退栈，局部变量失效，此时 Lambda 内部依然持有着那个引用的地址，一旦执行，立刻产生经典的悬垂/悬空引用导致崩溃。<br>**防范策略**：在转移所有权（向外 return 或给线程池）时，永远使用 `[=]` 按值捕获；在作用域内（通常作为 std::for_each 等同步算法使用）方可使用 `[&]` 进行修改以提升性能。",
      s: "离开作用域的回调严禁使用 [&]。",
      lv: "advanced"
    },
    {
      q: "C++14 的泛型 Lambda (Generic Lambda) 有什么特点？",
      a: "允许在 Lambda 的参数列表中直接使用 `auto` 关键字声明参数，不需要借助模板参数列表。",
      d: "`auto f = [](auto x, auto y) { return x + y; };`<br>这一特性大大增强了 Lambda 作为多态回调函数的能力。在底层，编译器对应的生成类 `operator()` 被改写为了一个成员模板函数。",
      s: "摆脱类型束缚，提供函数式轻量级泛型。",
      lv: "intermediate"
    },
    {
      q: "什么是 C++17 的结构化绑定 (Structured Binding)？",
      a: "允许一次性声明多个变量并从 pair、tuple、数组或者公共结构体中直接解构赋值出多个返回值。",
      d: "以前必须先声明：`auto p = myMap.insert({k, v}); bool ok = p.second;`<br>C++17 中：`auto [it, inserted] = myMap.insert({k, v});`<br>这直接绑定到了可读性更强的独立命名上。通常使用 `auto& [a, b]` 来绑定修改，结构化绑定的出现极大地简化了多返回值函数代码的编写复杂度。",
      s: "直接提取元组的各个字段，解放了极其繁琐的代码。",
      lv: "basic"
    },
    {
      q: "说说 C++17 的 if-init 语句（带初始化的 if 语句）的优点？",
      a: "允许在 if 条件判断之前的初始部分声明变量，该变量的作用域被严格限制在 if-else 块内部，避免了外部局部空间命名污染。",
      d: "`if (auto it = m.find(key); it != m.end()) { return it->second; }`<br>优点：控制生命周期是 C++ 开发非常重视的规范。让变量“越晚定义越好，并尽早消亡”。这样写变量就不必泄漏到上一层的作用域，提升了安全性和可读性，也方便重名变量在随后的其他块中使用。",
      s: "将寻找和判断集中到同一个语句块内。",
      lv: "basic"
    },
    {
      q: "介绍 C++17 中的 std::optional 及其应用场景？",
      a: "用于表示一个可能存在也可能不存在（为空）的值对象。通常取代使用魔法值（如返回 -1 或空指针）来代表“失败或未找到”的旧模式。",
      d: "`std::optional<int> findValue();`<br>它在堆栈内部包含一个 bool 标记和一个类型 T 的内部存储，没有额外动态内存分配。<br>通过 `opt.has_value()` 检测，并通过 `opt.value()` 获取。在设计清晰健全的系统 API 时是刚需工具。",
      s: "表达“有没有”的语义工具，解决了返回值安全问题。",
      lv: "intermediate"
    },
    {
      q: "C++17 的 std::variant 和传统 union 有什么区别？",
      a: "variant 是类型安全的 union。它能够保存它定义的候选类型中的任意之一，支持对象和存在非平凡(non-trivial)析构函数的类型，不再局限于底层 POD 数据。",
      d: "`std::variant<int, float, std::string> v = \"hello\";`<br>相比于传统的 union，传统 union 对具备深拷贝需求的对象无能为力且极易崩溃。variant 会妥善跟踪目前存储是哪一种类型，并在切换赋值时安全地执行旧类型的析构函数和新类型的构造函数。读取通过安全的 `std::visit` 或 `std::get<T>`。",
      s: "类型安全的多态存储解决方案。",
      lv: "advanced"
    },
    {
      q: "C++17 的 std::any 与 void* 的优势对比？",
      a: "std::any 能持有任意类型的数据并确保类型安全的提取，自动管理对象的生命周期；而 void* 没有类型安全性，只能进行生硬转换并且不负责生命周期析构。",
      d: "`std::any a = 1; a = std::string(\"haha\");`<br>如果使用 <code>std::any_cast&lt;int&gt;(&a)</code> 强转为错误的类型就会抛出异常。而 <code>void*</code> 转错类型则会导致随机未定义行为（核心转储）。",
      s: "任意数据类型安全装箱的技术支撑。",
      lv: "intermediate"
    },
    {
      q: "C++17 std::string_view 有什么极大优化点？有哪些风险雷区？",
      a: "优点是只持有内存首地址和长度（16字节），做字符串按范围读取和分割时零内存分配/零拷贝；缺点是寿命不自治容易发生悬挂，以及没有保证字符串必须是以 \0 结尾。",
      d: "当你把 `std::string` 传递为 `std::string_view`，根本没有任何深拷贝开销（常用于替换 `const std::string&` 或者 `const char*`）。<br>**最大风险**：它不具有自己内部缓冲的所有权。如果原 `std::string` 被销毁或重新扩容内存，`string_view` 就变成野指针。同时，由于可能表示字符串中间的一段，所以绝对不要强转当 C 标准库函数入参，否则可能读出越界数据。",
      s: "读字符串性能优化的利器，但是一定要配合严格的生命周期管控。",
      lv: "advanced"
    },
    {
      q: "简述 C++20 中 Concepts（概念）的作用？",
      a: "这是泛型编程(模板)的一大革命。为模板参数添加编译期的约束条件，使代码意图更清晰，避免产生数百行天书级别的冗余编译错误日志（如 SFINAE 的惨状）。",
      d: "示例：`template<typename T> requires std::integral<T> void foo(T n);`<br>在这里，编译器立马懂得这个接口仅接受整型（如int）。如果传入浮点型，报错非常干净：“不满足 std::integral 约束要求”。过去同样的功能需要依靠复杂的 `std::enable_if` 技术勉强实现。",
      s: "赋予了泛型编程（模板）自我规范的能力。",
      lv: "advanced"
    },
    {
      q: "C++20 的 Coroutines（协程）基本特点是什么？其状态保存在哪？",
      a: "C++20 引入了无栈协程。可以在挂起（co_await）期间让出线程交由其它任务执行。其局部变量和状态往往保存在堆上分配的“协程状态对象（Coroutine State）”中。",
      d: "无栈的意思并不是不用栈，而是不保留整个调用栈帧。协程的执行帧被压缩保存起来（往往是内存堆），当资源就绪/数据收到后恢复（resume）继续往下跑。<br>协程大大简化了以前严重依赖回调整合的地狱式异步网络编程架构（如 asio 回调链能被写成仿佛类似同步语句的流程）。",
      s: "异步编程的终极范式，通过极低的上下文切换实现高并发。",
      lv: "advanced"
    },
    {
      q: "C++20 的 Ranges（范围库）带来的代码改良指什么？",
      a: "它抽象了迭代器的复杂度，使得针对容器的过滤（filter）转换（transform）能够在一行之中利用管道 `|` 操作符进行无头组合，形成延迟计算数据流的函数式编程范式。",
      d: "`auto result = data | std::views::filter([](int i){return i%2==0;}) | std::views::transform([](int i){return i*i;});`<br>这并非立马生成多份无用的中间临时容器（像过去的 copy_if 必须声明临时变量），而是提供一种懒加载视图（ views）。在最终遍历的那一刻，底层计算才会真正被组合触发，极大降低了内存分配和消耗。",
      s: "提供优雅、延迟计算式的函数式链式数据处理。",
      lv: "intermediate"
    },
    {
      q: "C++20 的 Modules（模块机制）旨在解决 #include 带来的什么问题？",
      a: "解决头文件隔离不彻底，相互污染，特别是海量预处理重复宏展开而引发的天文级别的超慢编译构建时间。",
      d: "使用 `import std;` 而非 `#include <iostream>`。<br>模块在编译期能保存预先生成的内部抽象树数据结构。当多次 import 该模块时，编译器不再需要重新解析头文件的几千上万行文本，显著加快编译时间；并且内外部函数相互隔绝，解决了宏定义对外部的影响。",
      s: "C++ 现代化改造的最后一块拼图：解决构建时间。但企业级包支持落地仍需时日。",
      lv: "basic"
    },
    {
      q: "C++17 引入的 Inline 变量功能解决的核心痛点是什么？",
      a: "解决了在多个编译单元(cpp文件)中，在头文件声明和定义共享全局类静态变量或常量所导致的 “Multiple Definition” (多重定义) 链接层报错。",
      d: "过去要在类中设置一个统一的独立初始化静态变量，在 `.h` 文件中声明之后，还必须找到唯一的一个 `.cpp` 文件写明 `int A::count = 0;`。有了 `inline static int count = 0;` 特性后，不仅可以在头文件的类的内部进行直接初始化，链接器也会自动合并它们，将其折叠收束到唯一的一块内存地址上，极大地支持了现代的 Header-Only 架构的设计思路。",
      s: "彻底解决了头文件级声明静态/全局变量的噩梦。",
      lv: "intermediate"
    },
    // Task 1.2: 指针对比、内存泄漏排查、RAII (26-50)
    {
      q: "简述 RAII 机制的核心思想？",
      a: "资源获取即初始化（Resource Acquisition Is Initialization）。利用 C++ 对象在栈上的生命周期进行资源的安全绑定，依靠析构函数自动释放资源。",
      d: "传统 C 语言中，获取资源（如打开文件、申请锁、分配内存）后，必须保证在所有退出路径上手动释放资源，容易出错（特别是有异常抛出时）。RAII 将资源的获取放在类的构造函数中，并在局部对象被销毁时，利用确定性推导的析构函数清理资源。标准库的 `std::unique_ptr`、`std::lock_guard` 都是其典型应用。",
      s: "C++ 实现零开销且绝对安全的资源管理的核心哲学。",
      lv: "basic"
    },
    {
      q: "std::unique_ptr 和 std::shared_ptr 的主要区别？",
      a: "unique_ptr 是独占所有权、没有额外开销；shared_ptr 是共享所有权、内部通过按线程安全的引用计数来控制生命周期。",
      d: "1. 性能：`unique_ptr` 在编译期优化后几乎等同于裸指针，属于零开销抽象，不能被拷贝，只能移动(`std::move`)；`shared_ptr` 由于包含一个控制块（存储强引用和弱引用计数，通常在堆上），且引用计数增减是原子操作，因此有一定的时间和内存开销。<br>2. 释放机制：`unique_ptr` 退栈立刻销毁；`shared_ptr` 所有强引用者都丢失或重置后，计数为 0 时才销毁。",
      s: "优先默认使用 unique_ptr，仅在明确需要共享所有权时才退格使用 shared_ptr。",
      lv: "basic"
    },
    {
      q: "std::weak_ptr 的主要作用是什么？能否直接通过它访问对象？",
      a: "主要用于打破 std::shared_ptr 造成的循环引用噩梦。它也是一种观察者，不增加强引用计数。不能直接访问对象，必须提升（lock）为 shared_ptr 之后再用。",
      d: "A和B各自拥有一个指向对方的 shared_ptr，会导致两个对象的引用计数永远降不到 0，这就是循环引用内存泄漏。把其中一方对另一方的指向改为 `std::weak_ptr` 解决。<br>调用 `weak_ptr::lock()` 会首先检查对象是否存活，存活才返回有效的 `shared_ptr`，否则返回空的 `shared_ptr`。",
      s: "只看不买的观察者，破除引用死锁。",
      lv: "intermediate"
    },
    {
      q: "为何极力推荐使用 std::make_shared 而非 new 结合 shared_ptr？",
      a: "消除内存碎片，提升分配效率，避免极个别情况下的初始化异常导致内存泄漏。",
      d: "写法：`std::make_shared<T>()`。<br>1. **性能**：若用 `shared_ptr<T>(new T())`，系统要分配两次内存（一次给 T 本身，一次给控制块引用计数）。`make_shared` 将对象内存与控制块内存合并为单次连续的大内存分配，节约时间、降低碎片率并有利于缓存预读（Cache friendly）。<br>2. **安全**：在早于 C++17 时代的复杂函数传参中，可避免因传参求值顺序未指定而产生的异常安全问题。",
      s: "合并内存分配，提升性能防止碎片化。",
      lv: "intermediate"
    },
    {
      q: "std::shared_ptr 的按引用计数改变操作是线程安全的吗？它的管理对象本身安全吗？",
      a: "它的控制块引用计数增减是线程安全（原子操作）的，但所管理的底层对象数据和指针本身的切换并非线程安全。",
      d: "如果是多线程并发进行拷贝或者析构 shared_ptr 本身，引用计数能保证底层管理正确加减，不会 double-free。<br>**危险区**：如果多线程争抢并调用托管对象的方法（如 `ptr->doSomething()` 内部数据竞态），或多线程对同一个共享的 `shared_ptr` 变量实体直接做赋值覆盖 `ptr = anotherPtr` 操作，是**极不安全**的，需要加锁或者使用 C++20 的 `std::atomic<std::shared_ptr>`。",
      s: "计数安全，但所指的数据与指向线缆不安全。",
      lv: "advanced"
    },
    {
      q: "针对 C/C++ 项目，常用的查找内存泄漏的手段与工具有哪些？",
      a: "代码 Review 层引入智能指针；运行时排查工具主要使用 Valgrind、AddressSanitizer（ASan）及 perf，结合 smaps/pmap。",
      d: "1. **AddressSanitizer (ASan)**：在 GCC/Clang 编译时加上 `-fsanitize=address`。它通过插桩与影子内存技术，以极低的运行时停顿消耗能准确定位越界、堆泄漏、UAF（Use-After-Free）。属于首选现代方案！<br>2. **Valgrind (Memcheck)**：不需要重新编译，属于虚拟机动态插译分析机制，精准但会让程序慢 10-50 倍。<br>3. **系统排查**：观察 `top/htop` 的居民集 (RES) 持续缓慢增长，拉取 `pmap -x PID` 寻找匿名大额映射区。",
      s: "新项目首推 ASan，老项目插不进去再考虑 Valgrind。",
      lv: "intermediate"
    },
    {
      q: "能够手动 delete 一个 unique_ptr.get() 获取到的裸指针吗？",
      a: "绝对不行。这会导致典型的由智能指针二次释放引发的 Double-Free 灾难。",
      d: "智能指针本身是对生命周期的承诺。如果你将其底层裸指针 `get()` 出来手动删除了，由于对象并不是 `release()` 释放归属权的，`unique_ptr` 对象并不知情，在它结束作用域调用析构时，依然会去 `delete` 该地址。多次释放导致堆管理器崩溃，这正是智能指针初学者最易犯错误之一。在绝大部分情形下，不要单独用 `.get()` 给可能发生存储的所有权外部系统。",
      s: "不要替包办婚姻干涉寿命。",
      lv: "basic"
    },
    {
      q: "unique_ptr 和 shared_ptr 怎样定义自定义释放器 (Custom Deleter)？有什么主要区别？",
      a: "自定义释放器可以让智能指针去管理动态内存以外的资源（如文件句柄、Socket 描述符）。unique_ptr 必须将释放器类型写在它的模板参数中，而 shared_ptr 只需要在构造函数按参数传递。",
      d: "`std::unique_ptr<FILE, decltype(&fclose)> p(fopen(\"x.txt\", \"r\"), &fclose);`<br>`std::shared_ptr<FILE> p(fopen(\"x.txt\", \"r\"), fclose);`<br>因为 unique_ptr 是零开销模型，其尺寸必须固定（指针+空基元优化的释放器），所以它要求指明释放器类型来编译；而 shared_ptr 控制块反正是单独动态分配的，它内部用了类型擦除（Type Erasure）技巧，不需要在模板类型里带上 Deleter 类型。",
      s: "一个因极致性能刻进基因（模板内），一个因动态多态包容万象。",
      lv: "advanced"
    },
    {
      q: "C++11 废除 std::auto_ptr 的根本原因是什么？",
      a: "auto_ptr 的拷贝语义（赋值和拷贝构造）暗地里劫持并偷换了所有权，极其不符合常规拷贝的预期，稍有不慎在容器传递间就会导致空指针崩溃。",
      d: "如果你写 `auto_ptr a = expr; auto_ptr b = a;`，结果是 `b` 持有了指针，而 `a` 被无声无息地置为空（剥夺所有权）了！它的本意正是为了实现移动（那会还没有右值引用的概念）。但在引入 C++11 `std::move` 和 `unique_ptr` 后，通过右值 `&&` 显式警告和处理权属转移，使过程变得严谨透明，所以这个奇门毒药被毫不留情地废除。",
      s: "在没有右值概念的时代强行实现所有权转移的畸形产物。",
      lv: "basic"
    },
    {
      q: "悬挂指针 (Dangling Pointer) 和野指针 (Wild Pointer) 的区别？",
      a: "野指针是没有初始化随机指向的指针。悬挂指针是曾经指向有效合法内存，但内存已经被释放，指针自己却没被置为空的指针。",
      d: "`int* p;` 是野指针（它指向上帝才知道的一块内存）。<br>`int* p = new int; delete p; // p没被设为nullptr`，此时的 `p` 即为悬空/悬挂指针。<br>防范野指针需严格初始化，防范悬挂指针除了必须养成对象销毁马上赋 nullptr 以外，最一劳永逸的做法还是利用 RAII 与智能指针。",
      s: "一个是没娘管，一个是娘走了不管。",
      lv: "basic"
    },
    {
      q: "new/delete 与 malloc/free 有什么本质的跨维度区别？",
      a: "new/delete 属于 C++ 运算符级别，具备调用对象构造、析构函数并执行类型检查及重载的能力；malloc/free 是 C 标准库函数，仅仅分配一堆无语义的原始字节。",
      d: "当使用 malloc 创建类对象，该对象的构造函数、虚函数表 (vptr) 都未被激发，使用时必直接 core。<br>`new` 实际上经历了三个步骤：调用分配器获取空间（内部通常调 malloc）、在获取的内存调取目标类的构造函数初始化、将指针带类型返回。`delete` 则是反转过程，先调对象的析构以释放它拿下的私产（文件、锁甚至子类），最后归还这块土地。",
      s: "一个盖建大楼有开业仪式（生命流转），一个只是单纯圈一块空地。",
      lv: "basic"
    },
    {
      q: "为什么需要区分使用 delete 和 delete[] 释放数组？",
      a: "主要为了正确执行数组中每个非基础类型（类）元素的析构函数。",
      d: "当执行 `new int[10]` 并只用 `delete` 也能勉强收回空间（因为编译器内部有时会优化或类型本身平凡析构无需执行），但这属于极其恶劣的未定义行为(UB)！<br>但假如是 `new MyClass[10]`，底层除了分配对象内存，通常在数组开头会放一个隐藏的前缀标识（cookie）记录到底有多少个对象。`delete[]` 会读取这个 cookie 然后倒序把10个析构依此调用。若不加 `[]` 强行释放，只会错误调用第一个对象的析构，后 9 个出现内存（或资源）泄漏，大概率会直接破坏堆管理器，引起立即报错（段错误）。",
      s: "严格配对，遵守标准，绝不抱侥幸心理。",
      lv: "intermediate"
    },
    {
      q: "什么是定位 new (Placement new)？用于解决什么性能瓶颈？",
      a: "允许我们在用户事先分配的指定有效内存块上原地构造对象，直接绕过了操作系统底层的内存分配（比如耗时的 syscall 与碎片整理）。",
      d: "语法：`new (已知内存指针) Type(参数);`<br>使用场景：极其频繁地销毁、创建对象时，通常我们在初始化阶段通过 malloc 一下子拿走 OS 的一大块（或者是栈内存），然后利用对象的生命周期循环在上面做覆盖替换，这在游戏引擎内存池（Memory Pool）、高频交易（HFT）极低延迟构造对象以及 ZeroMQ 消息缓存预存池中属于核武级别的优化底座。注意必须明确手动调 `ptr->~Type()` 析构不能调 delete。",
      s: "内存复用的最基石抓手，完全掌控对象诞生过程。",
      lv: "advanced"
    },
    {
      q: "什么是重载 operator new 和 operator delete？为什么我们需要重载？",
      a: "为单个特定的类定做一套私人配属的内存控制逻辑，替代系统中默认的那套泛用的 new/delete 以提升速度或追踪诊断。",
      d: "有时候程序的内存碎片被特定体积极小却大量生成的节点撑爆（如二叉树、无锁队伍链）。我们在类里覆写这两个成员静态函数 `static void* operator new(size_t){...} `，就可以使得该对象的生成不触发全局 C 库函数，只从属于自研建立好的小型对象缓存池（slab / memory pool）中快速领用和归还，或者在分配时插桩加入特殊的调试信息来排查 OOM。",
      s: "高级内存泄漏追查以及无锁节点化池的必经之梯。",
      lv: "advanced"
    },
    {
      q: "简述 std::allocator 的工作机制。为何 STL 容器使用它而非直接 new？",
      a: "分配器将内存的申请获取与对象的构造这两步彻底解耦。STL 使用它是为了追求极高执行效率，防止毫无必要的默认初始化干扰，以及支持自定义外部内存策略。",
      d: "假如我们需要扩容一个 `vector<int>` 到一百万个元素。如果使用 `new[]`，那意味着要在百忙之中立刻执行一百万次无聊的零初始化或默认构造函数！借用 `allocator` 技术方案：仅仅只要求获取内存（`allocate` 阶段：啥都不干就是拿地）。在之后实际添加元素时，才精确指定那一块进行对象的实例化（以前是 `construct`，现被泛型生命周期等取代）。这叫延迟构造！同时这也允许工程接入自研巨型大页内存块以配合底层 NUMA。",
      s: "业务剥离：OS底座搞地块的归地块，C++对象盖房子的归房子。",
      lv: "advanced"
    },
    {
      q: "详细说说内存对齐 (Memory Alignment) 及其重要性，C++中的控制手段？",
      a: "硬件 CPU 访问内存按照固定的字宽（如 4或8字节）成块读取取数据，强制要求结构体数据的启点地址位于它尺寸的倍数上，从而避免横跨两个访问块引发的灾难级延迟。",
      d: "对齐不良一方面浪费空间（结构体末尾塞 padding 垫片），重点是：当一个 double（8字节）没对齐横跨了总线两个不同的读字边界时，CPU 需发起两次取指令甚至引起内核级软件陷入修复（Bus Error，像ARM平台可能直接异常终止）。<br>控制手段：使用 `alignof(T)` 获取对齐要求，使用 `alignas(64)` 或属性指令强制声明。在性能极度敏感（如无锁缓存）时我们经常声明按缓存行（Cache Line 64字节）强行对齐来彻底杜绝伪共享伪刷新问题。",
      s: "用最廉价的内存垫背交换 CPU 十倍以上的总线吞吐。",
      lv: "advanced"
    },
    {
      q: "解释返回值优化（RVO / NRVO）以及如何被 C++ 标准化？",
      a: "编译器智能跳过无聊的隐式临时变量生成，直接在函数的外部（最终目标内存）原地构建属于我们返回的大型对象，彻底免除复制/移动的开销。",
      d: "比如返回 `BigData` 结构，早年（C++98 及其之前）编译器会在栈上建立临时量再深拷贝出外。<br>1. **RVO（匿名）**：`return BigData();`<br>2. **NRVO（具名）**：`BigData data; ... return data;`<br>C++17 将这门昔日作为编译厂商良心馈赠的优化规定为了强制标准（即保证了复制省略 Copy Elision）。这也允许我们无视对象甚至禁用了拷贝构造而强行返回其值。",
      s: "别操心把大对象通过指针传进传出当返回值，直接放手大胆 return。",
      lv: "intermediate"
    },
    {
      q: "Double Free（二次清理分析）通常是怎么发生的？排查思路是什么？",
      a: "重复释放同一块已归还给系统的内存引发堆腐败 (Heap Corruption)。一般多见于浅拷贝（拷贝控制不严包含同一个裸指针）或者异常流执行出错。",
      d: "如果对象内部持有裸堆指针 `p`。发生复制构造且没写深拷贝的话，原物体与副本的 `p` 共同指向堆地。两兄弟作用域销毁时都会挥其死神之刃去 delete 它。<br>排查思路：排第一的是上神器 ASan (AddressSanitizer)。如果没这条件，看 Coredump 时检查是否死在 `malloc_printerr`（glibc 检测到了损坏）。审查类是否有违反三五法则（缺自定义赋值和拷贝），检查智能指针强行 get 是否再被干预。",
      s: "缺乏拷贝逻辑管理能力的产物。",
      lv: "intermediate"
    },
    {
      q: "如果 C++ 对象在“构造函数”内部抛出异常，会发生什么后果？资源会泄漏吗？",
      a: "这个对象将被认为是未彻底完成构造的不合法存在，它的析构函数**绝不会**被后续触发！内部所有事先分配好的裸资源极易泄漏。",
      d: "假如对象里面包了 `int* a; int* b;`，如果在 `new b` 的时候抛出 `bad_alloc` 异常，因为这属于没出生成功，外部将得不到指针不能管它，本身的析构也就没有执行权，先前抢到的 `a` 彻底成孤儿。解决之道依然是利用智能指针：因为构造虽然整体失败中断不调用类析构，但是系统在抛出前**一定会展开销毁这期间已经创建好的栈内部成员对象**（`std::unique_ptr` 等），于是能够保证绝对安全撤退。",
      s: "构造一半即为死亡，安全唯独靠栈内 RAII 先发优势。",
      lv: "advanced"
    },
    {
      q: "如果 C++ 对象在“析构函数”内部抛出异常且抛出逃逸到外部，会发生什么后果？",
      a: "这属于万劫不复的严重忌讳，大概率触发 std::terminate 从而直接杀死全程序。",
      d: "大多数情况下对象的销毁都是系统（例如退栈）连带触发的，假如你此时原本由于遇到异常而正处在“回推寻找 catch 处理处”的大撤退路上（栈展开机制 Unwinding），这个时候半路上遭遇某个对象的析构函数偏偏又抛出新异常出圈！这会立刻由于 C++ 处理不了“双抛异常并发”状态，从而强制崩掉系统保证安全。C++11 开始析构函数被隐式附加上不可抛的声明 `noexcept`。",
      s: "严厉禁止并在析构函数处内部消化掉所有可能的报错。",
      lv: "advanced"
    },
    {
      q: "C++ 排查 OOM（Out Of Memory）的根本诱因？",
      a: "无非三点：真泄漏、大量碎片囤积不能用、请求额度本来就是无理要求。",
      d: "1. 内存泄漏（Leak）：用 Valgrind / ASan。长期缓慢泄出。<br>2. 碎片化导致申请失败（Fragmentation）：虽然统共余量大，但已被割离的千疮百孔分配不出巨大连块。用 jemalloc / tcmalloc 能够极好缓解。排查可以通过 `cat /proc/PID/status` 看 VmData。<br>3. 大跨步扩容（容器乘载灾难）：向 vector 推入极其庞大的非预估量对象发生加倍时申请 2x 体积炸服。要用 `reserve` 先见性或者分批改存 `deque/list`，控制容量爆炸。",
      s: "OOM 不一定是真的泄了，有可能是系统吃饱撑着不给（碎片），或者狮子大开口（扩容成倍暴增）。",
      lv: "intermediate"
    },
    {
      q: "深拷贝和浅拷贝的本质区别是什么？",
      a: "浅拷贝是无脑的一级字段字节位复制直接抄袭；深拷贝是深入进去去分配独立的具有相同状态内容的新内存储块。",
      d: "当我们有一个包含裸指针的 Class 进行拷贝时，系统默认生成的通常是浅拷贝拷贝构造函数，这导致旧指针与新指针对着同样的房子开门，会带来悬挂释放和共享腐败。一旦类涉及自行掌控资源，必须主动写明**“三五法则”**中的所有逻辑去处理深拷贝，把指向的数据拿份独立的拓本出来。",
      s: "深拷贝抄的是精华魂魄，浅拷贝抄的只是一本花名册指针。",
      lv: "basic"
    },
    {
      q: "能详述一下智能指针与普通内部成员函数 this 的生命周期纠葛 - std::enable_shared_from_this 吗？",
      a: "如果在类方法中必须向外抛送该类对象的 shared_ptr，不能强行用 this 指针直接封装出新的。必须继承此特性安全提拽。用于解决共享引用的双重管理隔离崩盘问题。",
      d: "如果你在 `doWork()` 中写了一句 `auto ptr = std::shared_ptr<MyClass>(this);` 准备提交到线程池执行。这就彻底完了，因为这意味着你为原来的 `this` 在外部开创了一套**全新的，互不相干的独立引用计数控制块**。<br>解决方案：基类继承 `std::enable_shared_from_this<MyClass>`。然后调用内部已经构建好的法器 `shared_from_this()`，它能悄无声息地从对象内部挖掘出并复用外部建立好的原配控制块。",
      s: "绝不能用裸 this 去做智指代领，请通过正规法定途径（enable_shared_from_this）。",
      lv: "advanced"
    },
    {
      q: "在实际高压开发中，智能指针的滥用会引发什么核心灾难？",
      a: "庞大的内存开销、严重的控制块竞占拖垮并发速度、隐藏掉循环依赖关系导致更难复现的隐性泄露深渊。",
      d: "智能并非免费。如果是 `shared_ptr` 满天飞当传家宝：这需要频繁的在 CPU 总线中去发大招竞争（锁总线的原子 CAS 操作），在多核高频流水账处理阶段会被严重的竞争消耗殆尽。正确做法应是：只在寿命必须共享延伸的关键接缝使用 `shared_ptr`，其余环节使用常引用 `const shared_ptr&` 或剥离裸指针/视图去进行运算。",
      s: "智能并不免单，到处乱拷贝 shared_ptr 极其拖延速度。",
      lv: "advanced"
    },
    {
      q: "为什么禁止包含裸类型指针或引用的结构体执行直接 memcpy?",
      a: "memcpy 属于二进制的野鸭强上操作，毫不关切语义与析构，破坏对象的封闭生命力并极大概率诱发奔溃灾难。",
      d: "它完全无视了 C++ 内置任何特性的生命运转轮回逻辑（跳过了繁华的构造、连计数增加都没发生）。拷贝后你获得了虚假的地址映像。若是包含虚函数，在不同的编译上下文还可能破坏掉虚表的链接点。并且这是彻头彻尾的浅拷贝，它使得 RAII 系统认为这个对象完全不存在。除非经过严格 `std::is_trivially_copyable` 检验判明为平庸数据 POD 块，严禁这样用作对象。",
      s: "绝对禁止对有脑子的生命体用外科物理刀强制移植。",
      lv: "intermediate"
    },
    // Task 1.3: 模板元编程基础、STL 容器底层数据结构与扩容原理 (51-70)
    {
      q: "简述 std::vector 的底层实现原理和自动扩容机制？",
      a: "底层是一片连续分配的动态内存数组（3个指针：begin, end, end_of_capacity）。当插入超出容量时，通常会以 1.5倍 或 2倍 的策略重新开辟新内存、移动内容并销毁老内存。",
      d: "不同编译器的扩容系数不同（GCC 是 2 倍，MSVC 常见 1.5 倍）。<br>扩容时如果是平庸的拷贝，会大面积消耗时间；如果其中对象的移动构造函数标有 `noexcept`，则 vector 扩容时会使用最高效的移动构造避免了大量深拷贝！",
      s: "连续存储的伸缩容器。扩容性能极其依赖元素的右值移动支持。",
      lv: "basic"
    },
    {
      q: "为什么大体量高频搜索时往往推荐 vector 配合二分查找，而不是强用 std::list 或 std::set？",
      a: "因为 CPU 基于连续内存会提供极其高效的 Cache 预读行为（空间碎片局部性好）。指针跨越跳跃会导致缓存频频未命中（Cache Miss）。",
      d: "`std::vector` 的内容是排在一起的物理内存块，读取第 1 个字时，L1 缓存通常会顺便把后续的几十个字节拖进去加载。而 `std::list` / `std::set(红黑树)` 每一个元素内存地址在系统上都是离散分布的。一旦进行遍历，每一次下跳都是一次 CPU 高昂成本的去主存中 Fetch (Cache Miss)。所以即便存在移动删除代价，连续数组也是常态下搜索表现最好的。",
      s: "不要和 CPU 缓存友好度作对，连续存储碾压一切离散指针结构。",
      lv: "intermediate"
    },
    {
      q: "说说 std::vector 的迭代器失效问题？",
      a: "如果在迭代期间删除了元素或插入引发扩容，该点及之后（或全局）的所有迭代器都会变成废指针引起奔溃。",
      d: "1. 插入：若超过容量引发了 `reallocate` 内存大搬家，所有先前的迭代器瞬间全灭。若未触发扩容，插入点之后的迭代器由于数据后移而全部失效。<br>2. 删除：被删元素及位置之后的所有迭代器全挂。<br>**经典解法**：`erase/insert` 均会返回操作发生后的下一个合法首轮迭代器。利用 `it = vec.erase(it);` 来保持更新。",
      s: "改变数组形状即可能毁灭所有游标指示物，要时刻靠函数返回值抓取新游标。",
      lv: "intermediate"
    },
    {
      q: "那 std::map 的迭代器什么时候失效？怎样正确删除正在遍历的元素？",
      a: "map 基于红黑树结点构建，对内部进行插入或删除操作时，只有真正被剔除的那个当前元素的迭代器会完蛋死翘翘，其余不影响。",
      d: "因为都是散装的节点借树形挂接着，删一个对同伴的物理住址无关。如果是 C++11 前常用这个技巧：`map.erase(it++)`（拿旧拷贝去删，真身向后越迁）。C++11 后 `erase` 直接返回下个有效迭代器了：写法变为 `it = map.erase(it);`。",
      s: "散装结构牵连小，删谁谁死。不过 C++11 后语法都高度统一为返回下一个迭代器了。",
      lv: "basic"
    },
    {
      q: "std::map 和 std::unordered_map 的底层选型对比与适用场景？",
      a: "前者底层主要是红黑树，查询代价 O(log n)，元素严格按 Key 排序。后者是哈希表，平均查询代价 O(1)，无序分布。",
      d: "`unordered_map` 使用开链法（挂链表）或开放寻址来处理哈希碰撞问题。在元素规模巨大并且不具备按范围读取查找需求（比如想获取最大 10 项）时，`unordered_map` 能展现惊艳的高吞吐查询效率。但也要求你要妥善实现或利用强大的泛用 Hash 散列函数，如果散列不均匀引发所有元素挂一条长链上，它不仅没 O(1) 还能被拖成 O(N) 的灾难。",
      s: "二者分别主宰着保序场景与纯粹的海量点查场景。",
      lv: "intermediate"
    },
    {
      q: "当 unordered_map 遇到严重哈希碰撞时，底层是怎么处理的？",
      a: "使用链地址法（拉链法）。同一个桶里的多个冲突元素串联在一起。若严重超量导致整体负载因子过高会触发整表 Rehash（重哈希）。",
      d: "每个桶底层是一个链表（或红黑树化）。当当前容器中总元素数量超过了 `桶数量 * max_load_factor(默认1.0)` 时，分配全新两倍数量的巨型表块阵列，遍历一切旧表数据一个一个计算新的 Hash 桶归属并插进去。这是一个极其占用 CPU 与耗时的操作（Stop The World 级）。开发时提前 `reserve` 个大概数量是不变真理。",
      s: "要么挂链条妥协，要么拉大表重构，必须用 reserve 防一手。",
      lv: "intermediate"
    },
    {
      q: "std::deque 的底层数据存储模型是什么？有何特点？",
      a: "采用分段连续分配数组（往往称为 Chunk / Buffer 控制块）加中心映射表结合的设计实现。是 vector 和 list 之间的混合产物。",
      d: "它是一堆块组成的。每一块里可能是存储的 100个 T。外部维护一个装有这许多块指针数组（Map）。这种打碎又联合的设计，让它可以拥有在前后两端均以看似 O(1) 开销塞入元素的能领，不会像 vector 插前头大灾难，也不会像 list 一样指针爆炸。通常被用来担当 `queue`（队列）和 `stack`（栈）的底层被包涵标准类。",
      s: "缝合了数组和散碎指针，支持两端高效推进的折中霸主。",
      lv: "advanced"
    },
    {
      q: "为什么经常提倡用 emplace_back() 替换 vector 的 push_back()？",
      a: "emplace 会在底层内存上直接传入构造函数的参数并发起原位生成。省去了隐式转化构成的临时对象的复制或移动步骤。",
      d: "`vec.push_back(MyObj(1, 2))`：发生的过程是构建了一个 `MyObj` 临时生命，然后再复制（或依靠C++11移动）到那块区域内部。<br>`vec.emplace_back(1, 2)`：不需要外部对象，直接拿着参数 `1和2` 在向量深处通过 `placement new` 进行调用构造函数一次点亮。这是极限压榨运行期的体现。",
      s: "直接邮寄材料在终点现场盖楼，而不是在路边盖好再搬迁过去。",
      lv: "intermediate"
    },
    {
      q: "详述模板开发中 SFINAE 准则是什么含义？",
      a: "Substitution Failure Is Not An Error（匹配失败并不是一次错误）。当编译器将具体类型替入模板引发不合法代码时，编译器会默默忽略这条重载模板去找下一个，而不会强硬报错中断。",
      d: "这是各种极具抽象魔力的老式类型萃取库（Type Traits）的底层工作源泉。可以通过 `std::enable_if` 让某项特定模板专门服务于含有具体性质的对象，如果别人也调用了，SFINAE 就自动把这版函数剔除出考察队列外而不引起灾难级别的错误。<br>（注：此技能由于极难掌握，目前在 C++20 中已被 **Concepts** 半壁替代）。",
      s: "不合适就默默走开换下一家的温柔（模板核心工作机制）。",
      lv: "advanced"
    },
    {
      q: "C++ 变参模板 (Variadic Templates) 解决了老代码什么难题？",
      a: "使得模板类/函数能够不受限制地接收零个或漫天海量任意参数类型组合，彻底粉碎了老标准为了泛型写满屏重载的丑陋形式。",
      d: "形式：`template <typename... Args> void log(Args... args);`<br>其内部解包工作在编译时期主要依赖于极其经典的**“基础函数截停+递归推演”**（利用展开剩余剩余参数不断自身调用），或者在 C++17 后应用简单无脑的折叠表达式 `(cout << ... << args);` 一波带走执行。在现代打车库底层实现非常多。",
      s: "不限制类型、无边界数量的终极泛型装载工具。",
      lv: "advanced"
    },
    {
      q: "标准库中仿函数/函数对象 (Functor) 与普通函数指针的区别与优点？",
      a: "重载了 `operator()` 方法的一个普通类实例。最大的优点是可以拥有各种类型的内嵌持久局部私用状态（由于是类），且极易被编译器极致内联展开消除开销。",
      d: "普通指向函数的指针没办法在身上常留特殊记忆（除非上非常丑且难维护的全局静态态）。如果是类：`struct Adder { int c; int operator()(int a, int b) { return c*(a+b); } };` 有天然的参数装配。其次，传统的指针极难穿越黑盒（如 `std::sort` 内部），编译器很难预测，而仿函数类型分明、显式可见，几乎全军被 `inline` 化。",
      s: "带了状态且能够向编译器掏心掏肺的超级函数。",
      lv: "intermediate"
    },
    {
      q: "std::function 相较于原始的 lambda 或 auto，开销体现在哪里？",
      a: "它采用了类型擦除（Type Erasure），是一种多态包装器，会有内部的堆内存分配及虚函数调用开销。",
      d: "编译器能精确了解 lambda 本身是哪个闭包类，用模板直接展开为极快调用。如果你把 lambda 塞进了 `std::function<void()>` 以供异构容器（如保存全部种类回调函数的数组）统一调用。这时函数内部为了装箱就会把你的对象塞入堆上并挂接一个多态虚调表来强行统合面貌（如果体积极小能触发SBO小对象优化算幸存）。使用在绝对不能损失一点毫秒级响应的高频内核地带必须谨慎对待。",
      s: "万金油包装器，代价是虚函数下发带来的额外时间及内存碎片。",
      lv: "advanced"
    },
    {
      q: "std::bind 的主要应用在现今为何逐渐被废弃不用？被什么替代？",
      a: "因为 Lambda 出现！Lambda 可读性强一万倍，支持编译期自动捕获并压制传参开销。",
      d: "`auto f = std::bind(&MyClass::func, this, std::placeholders::_1, 100);` 里面那些长长短短难看的预设槽位绑定不仅冗长，更是强行在运行前制造了隐晦的临时拷贝；若直接用 `auto f = [this](int a){ func(a, 100); };` 书写无比直观干练且不触发多此一举封装。",
      s: "昔年绑定神器被完美优雅的 Lambda 全方位扫进历史垃圾堆。",
      lv: "basic"
    },
    {
      q: "std::priority_queue (优先队列) 底层使用何种数据结构？用来解决什么核心算法问题？",
      a: "底层基于最大/最小堆结构（基于 vector 或 deque 实现）。最擅长抓取并保持全局目前优先级最高/最高分（如寻找Top K，定时器超时事件提取）的内容操作。",
      d: "其实是个用 `std::make_heap / push_heap / pop_heap` 在一个动态数组里假装树形游走的算法皮包。你送入一个无序数字，他并非全部 O(NlogN) 排序，他只是 O(logN) 上推调整保证堆顶位置一定站着这群元素的“王”。非常适合处理在百万大军里源源不断只切拿走等级前十的硬骨头。",
      s: "无心全局排序只在一隅做王的高效收割器。",
      lv: "intermediate"
    },
    {
      q: "在 C++14 后加入的 decltype(auto) 拥有何种独特的解决场景？",
      a: "主要为了在做通用全能模板（如将别人的返回值毫无改变打包出去的适配器）返回值时，做到 100% 精确保留原返回值里所有的引用与 Const 性质而加进来的。",
      d: "你写个辅助函数返回结果：如果是 `auto`，很惨它会自动剥夺引用把数据按值拷贝导致深层错误；如果是被 `decltype()` 强行括着原表达却导致写多一遍重复的巨大串逻辑。`decltype(auto) val = func()` 会按原样接住 func 所有性质返回，做高阶泛用转发基石。",
      s: "解决模板万能函数包装转发时引用属性意外被扒光的利器。",
      lv: "advanced"
    },
    {
      q: "通常建议将模板类的定义和声明分离在 .h 和 .cpp 里面写吗？",
      a: "这在历史上是个不可调和的雷区，绝对不能将实现扔在独立的源文件中去（若非全部用外部进行显式实例化处理）。必须全都包在头文件里发布。",
      d: "编译器面对 C++ 文件是进行孤岛作业分别翻译的！当其它模块调用一个模板类型并填入了 `<int>` 类型时，只要能在头文件那找到定义即能实例化编译出一版带类型的类代码段。如果你藏在另一个 `.cpp` 里，当下这个正调用它的文件就会干瞪眼没法特化它发生致命地找不到符号错误（链接前）。目前 C++20 的模块(Modules)技术正是消杀该痛点的武器之一。",
      s: "别瞎分开，全塞头文件（.h 或 .hpp），否则编译链接炸飞你。",
      lv: "basic"
    },
    {
      q: "简述 std::array 与 C 语言内建原生数组的本质区别？",
      a: "std::array 其实是在原生数组外包了一层轻量外包装类，没有运行时多余开销并且不会在传参时退化成为一个“光杆指针”（带全了尺寸并享受值传递深拷语义）。",
      d: "如果你向深处传一个 `int A[50]`，到了底下就会可悲地退化成了 `int*` 丧失数组一切边界尺寸认知。而作为零开销包装层的 `std::array<int, 50>` 会把完整的体型在泛型接口交棒。并且其配备了与其余 STL 绝配对齐的迭代器等基础构件。",
      s: "加了最稳重的锁链并拒绝退化掉身价阶层 C 素体。",
      lv: "basic"
    },
    {
      q: "什么叫做 C++ 的擦除删除惯用法 (Erase-Remove Idiom)？针对 vector 或 string 有什么作用？",
      a: "用于极其高效地将特定条件的元素一口气在底层连续数组内部剔除而避免反复发生大段内存后移挤压的方法论。",
      d: "`vec.erase(std::remove(vec.begin(), vec.end(), target), vec.end());`<br>由于 `std::remove` 实际并未执行毁灭行为（它是无害算法不能侵略容器空间），它只是疯狂地把不该删的数据快快乐乐挨个往前复制覆盖（O(N)次），最后停下处作为垃圾区首针返回。然后靠 `erase(从垃圾堆，直到真正终点)` 一瞬间彻底销毁后面多余空位。在 C++20 引进了简单统一的 `std::erase / std::erase_if` 将其隐去。",
      s: "算法不管清理物理垃圾只负责覆盖，容器配合清理才能完结。",
      lv: "intermediate"
    },
    {
      q: "C++11的 typename 和 class 在定义模板参数 (template <...>) 时有什么区别？用在哪里有绝对区别？",
      a: "在单纯写在角括号里表意是毫无区别的。但在表达“嵌套的依赖类型”时，为了安抚编译器避免发生其被误解成静态变量，必须强制使用 typename。",
      d: "`template <typename T>` 等同于 `template <class T>`，看个人口味。但在比如获取迭代器时 `T::iterator it;` 这里因为 T 没确立，编译器默认这也许是 T 里面的某静态数值（像做乘法）。所以如果你明确它是个内部子类型就必须在前面敲实 `typename T::iterator it;` 来制止迷网报错。",
      s: "括号里互换无罪，破处嵌套类型迷魂阵时非他莫属。",
      lv: "advanced"
    },
    {
      q: "解释类型萃取技术详解 (Type Traits - 位于 <type_traits>) 解决什么工程难题？",
      a: "如同编译期执行的反照术。它的主要用途就是在浩瀚的实例化构建发生之前，审查摸清这些塞入来的类型是不是算数类、要不要做平庸拷贝以分发最优方案。",
      d: "包含诸如 `std::is_pointer<T>::value` 和 `std::is_trivially_copyable<T>` 获取返回（皆在编译期跑完）。配合刚刚聊过的 SFINAE 等手段，STL 如果看到对象是数字能被生按 byte 拷贝，底层就会被分发流管送进极致暴躁快速的 `memcpy` 车间进行拷贝；如果是老顽固对象则调老路线遍历拷贝构架（`std::copy`的内幕！）。",
      s: "编译期的侦探放大镜，提供最智能的性能优化分发凭证。",
      lv: "advanced"
    }
  ]
};


