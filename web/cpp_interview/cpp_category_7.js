const cppQuestions_Category7 = {
  "network_gateway_security": [
    // Task 7.1: SD-WAN 网关、TCP/UDP 流表、TLS 指纹与高速转发 (1-36)
    {
      q: "为什么网络网关里的会话表通常用五元组做 Key？双向流该怎么归一化？",
      a: "五元组是源 IP、目的 IP、源端口、目的端口和传输层协议，能稳定区分一条 L4 会话。双向流通常把正反方向归到同一个 flow entry，并在 entry 内记录方向位、原始五元组和反向五元组。",
      d: "如果不做方向归一化，客户端到服务端和服务端到客户端会落进两条记录，状态机、字节统计、NAT 映射和 DPI 结果都容易分裂。常见做法是按协议、IP、端口构造 canonical key，例如小地址/端口放前面，同时保存 original/reply tuple。Linux conntrack 也把原始方向与回复方向分开保存。<br>工程上还要考虑 VLAN、VRF、zone、入接口等扩展维度，否则多租户或多出口环境下五元组可能碰撞。",
      s: "面试官问五元组，本质是在问你能不能把连接识别、方向归并、NAT 和多租户隔离一次性想清楚。",
      lv: "basic"
    },
    {
      q: "TCP 是字节流而不是消息流，这对网关做协议识别有什么影响？",
      a: "一次 recv 或一个 TCP segment 不等于一个完整应用层报文。网关必须按序列号重组成连续字节流，再按 HTTP/TLS/DNS 等上层协议自己的长度字段或语法边界解析。",
      d: "典型错误是看到一个 packet 就尝试解析完整 HTTP Header 或 TLS ClientHello。现实里一个 TLS record 可以被拆成多个 TCP segment，也可能多个 record 合并在一个 segment 里。正确设计需要每个方向维护 next_seq、已到达区间、缺口和应用层解析游标。<br>对代理服务而言，内核 TCP 已经给应用交付有序字节；对旁路 DPI/IDS 而言，你看到的是原始包，必须自己处理乱序、重传、重叠片段和抓包丢包。",
      s: "TCP 给应用的是水管，不是一箱箱快递；网关如果按快递盒拆协议，遇到分片和合包就会翻车。",
      lv: "intermediate"
    },
    {
      q: "TCP 重组时遇到乱序包、重传包和重叠片段，应如何处理才不会被绕过？",
      a: "维护按序列号排序的区间缓存；乱序包先缓存，缺口补齐后推进解析；重传包去重；重叠片段必须采用与被保护主机 TCP 栈一致的覆盖策略，并设置内存和时间上限。",
      d: "IDS/IPS 里最危险的是 evasive traffic：攻击者利用不同操作系统对重叠 TCP 片段的处理差异，让 IDS 看到的字节流和真实主机看到的不一样。实现时不能只做简单拼接，必须记录区间、判断 overlap、限制 out-of-order 队列长度，并在流长期缺口时降级或丢弃。<br>同时要区分重传与新数据：相同 seq 和 payload 是重传；相同 seq 但 payload 不同必须计入异常事件。",
      s: "能讲清楚重叠片段策略的人，通常真的做过 DPI/IDS 或至少认真读过相关实现。",
      lv: "advanced"
    },
    {
      q: "TCP 状态机在网关流表生命周期管理里有什么实际价值？",
      a: "它决定流什么时候创建、进入 established、半关闭、异常关闭和回收。网关依赖状态机选择超时时间、允许的报文方向、是否建立 NAT/安全策略状态。",
      d: "SYN 建流后通常使用较短的半连接超时；三次握手完成后切到 established 长超时；FIN 双向完成或 RST 出现后进入短回收。只按固定 idle timeout 管所有 TCP 流会导致 SYN flood 占满表、长连接被误删、异常连接长时间占资源。<br>防火墙还会基于状态判断 out-of-state 包，例如没有见过 SYN 却突然出现 ACK 的包通常不应建立新会话。",
      s: "TCP 状态机不是书本八股，在状态防火墙和网关 NAT 里就是流表的生老病死规则。",
      lv: "intermediate"
    },
    {
      q: "SYN Flood 为什么能打爆服务端？网关层常见缓解方案有哪些？",
      a: "攻击者大量发送 SYN 但不完成握手，消耗服务端半连接队列和状态资源。缓解手段包括 SYN cookies、SYN proxy、速率限制、源信誉、黑白名单和半连接超时调优。",
      d: "普通 TCP 服务端收到 SYN 后要分配半连接状态并回复 SYN-ACK。攻击者伪造源地址时，服务端等不到第三次 ACK，队列被占满后正常用户无法连接。SYN cookies 把状态编码进序列号，SYN proxy 则由网关代替后端完成握手，确认客户端真实存在后再连后端。<br>工程上不能只靠一个开关，还要监控 syn_recv 数、backlog 丢弃、重传率和源分布。",
      s: "这类问题常出现在防火墙、SLB、SD-WAN 网关岗位，用来判断你是否理解 L4 防护的资源消耗模型。",
      lv: "intermediate"
    },
    {
      q: "TCP 的快速重传和 RTO 超时重传有什么区别？抓包时怎么看？",
      a: "快速重传由重复 ACK 触发，说明接收端持续收到后续数据但中间有缺口；RTO 是重传定时器超时，通常表示 ACK 长时间没回来或路径严重丢包。",
      d: "快速重传通常伴随多个 duplicate ACK，发送端不必等超时就补发缺失段；RTO 则会更重地收缩拥塞窗口，吞吐下降更明显。Wireshark 的 TCP analysis 标记是启发式结果，不能盲信，要结合 seq/ack、capture point、网卡 offload 和是否丢包一起判断。<br>如果在发送端看到发包但收不到 ACK，可能是回程丢；在接收端看到缺口，可能是正向丢；只有单点抓包往往无法给出完整结论。",
      s: "面试里让你分析 pcap，不是背 Wireshark 标签，而是用序列号和 ACK 还原现场。",
      lv: "intermediate"
    },
    {
      q: "TIME_WAIT 为什么不能随便调得很短？它对高并发网关有什么影响？",
      a: "TIME_WAIT 用于确保旧连接残留报文自然消失，并让最后一个 ACK 有机会重传。调得过短可能导致四元组复用后旧包污染新连接。",
      d: "主动关闭方进入 TIME_WAIT，持续 2MSL。高并发短连接场景下，TIME_WAIT 会占用端口和内核表项，但它不是单纯的垃圾状态。网关如果做 SNAT，源端口池耗尽才是核心风险，需要端口复用策略、连接复用、扩大源地址池和合理超时配合。<br>粗暴打开危险复用参数可能短期压住指标，长期制造难复现的串包和偶发连接失败。",
      s: "TIME_WAIT 是 TCP 用时间换正确性的一道保险，不懂它就很难设计稳定的四层网关。",
      lv: "intermediate"
    },
    {
      q: "UDP 没有连接，为什么防火墙和 SD-WAN 网关仍然要维护 UDP 流表？",
      a: "为了做 NAT 映射、回包放行、统计、QoS、路径选择和应用识别。UDP 流表靠五元组和 idle timeout 维护软状态，而不是靠握手状态。",
      d: "DNS、QUIC、VoIP、游戏都大量使用 UDP。网关需要记住某个内网源端口映射到哪个公网端口，也要知道短时间内哪个反向包属于已允许的会话。不同 UDP 应用超时不同：DNS 可以很短，QUIC/隧道要更长。<br>如果把 UDP 都当成无状态包处理，NAT 回包、应用统计和安全策略都会失去上下文。",
      s: "UDP 无连接不等于网关无状态，软状态表是工程系统补出来的连接感。",
      lv: "basic"
    },
    {
      q: "IPv4 分片和 TCP 分段有什么区别？DPI 为什么经常要先做 IP 重组？",
      a: "TCP 分段发生在传输层，用序列号恢复字节流；IP 分片发生在网络层，同一个 IP 包被拆成多个 fragment。DPI 要看 L4 端口和 payload 时，必须先处理 IP 分片，否则后续分片可能没有 TCP/UDP 头。",
      d: "IPv4 分片用 identification、fragment offset 和 MF 标志重组。攻击者可能利用 tiny fragment、重叠 fragment 绕过防火墙规则。网关一般要在进入 conntrack 或 DPI 前完成 defrag，并限制分片缓存资源。<br>IPv6 中路由器不做中途分片，依赖源端和 Path MTU Discovery，但安全设备仍要处理扩展头和分片头带来的解析复杂度。",
      s: "看不懂分片，很多 L4/L7 规则都会只检查到第一片，后面的攻击载荷直接溜过去。",
      lv: "advanced"
    },
    {
      q: "什么是 Netfilter conntrack？它和自研网关流表的设计有什么相通点？",
      a: "conntrack 是 Linux Netfilter 的连接跟踪子系统，记录原始方向和回复方向 tuple、协议状态、超时、NAT 信息和计数。自研流表也要解决 key、状态、超时、方向、并发和回收这些问题。",
      d: "conntrack 不是只跟 TCP，UDP、ICMP 也会有软状态。它为 stateful firewall、NAT 和 flow offload 提供基础上下文。自研网关如果绕开内核实现高速路径，也需要类似的数据结构：hash bucket、per-zone 隔离、LRU/定时轮、协议状态机、统计字段和异常标记。<br>区别是自研系统可以按业务裁剪字段，并把同一 flow 固定到同一 worker 降低锁竞争。",
      s: "Linux conntrack 是流表设计的公开参考答案，能讲透它基本就能讲透网关状态面。",
      lv: "intermediate"
    },
    {
      q: "网关流表满了以后应该怎么淘汰，为什么不能简单随机删除？",
      a: "应按协议状态、业务优先级、最近活跃时间、风险评分和资源配额分层淘汰。随机删除会误杀正常长连接，也可能让攻击流挤掉关键业务流。",
      d: "常见策略是优先清理 embryonic TCP、长期 idle 的 UDP、低优先级租户或低价值流；established 大流可以使用更长 timeout，但也要受全局配额约束。实现上常用分片 LRU、时间轮、最小堆或 per-bucket 链表。<br>还要避免淘汰线程和转发线程争同一把大锁，常见做法是 per-core flow shard 加批量回收。",
      s: "流表满是网关产品迟早会遇到的战场，淘汰策略直接决定故障时保谁、丢谁。",
      lv: "advanced"
    },
    {
      q: "TLS 握手里 ClientHello 对加密流量识别有什么价值？",
      a: "ClientHello 在传统 TLS 中包含版本、cipher suites、extensions、ALPN、SNI 等元数据，很多字段在应用数据加密前可见，因此常被用于站点识别、客户端指纹和策略匹配。",
      d: "TLS 1.2 和 1.3 都会先协商参数，再进入加密应用数据。SNI 能告诉服务器客户端想访问的域名，ALPN 常暴露 HTTP/2、HTTP/1.1 等应用协议，cipher/extension 组合可形成 TLS 指纹。<br>但 TLS 1.3、ECH、QUIC 和浏览器指纹随机化正在减少明文元数据，产品不能假设永远能看到完整域名和稳定指纹。",
      s: "加密流量识别拼的不是解密全部内容，而是在握手阶段抓住还没被盖住的元信息。",
      lv: "basic"
    },
    {
      q: "SNI 能解决什么问题？为什么只靠 SNI 做域名识别越来越不可靠？",
      a: "SNI 让客户端在 TLS ClientHello 里声明目标域名，便于同一 IP 上多个 HTTPS 站点选择正确证书。但它可能缺失、被伪造、被 CDN 共享域名模糊化，且 ECH 会加密真实 ClientHello。",
      d: "传统 HTTPS 中，HTTP Host 头在 TLS 之后才出现，服务器需要更早知道证书该选哪个，所以 SNI 被放进 ClientHello。对网关来说，SNI 是低成本域名识别信号。<br>问题是 SNI 不是强认证字段：恶意客户端可填假值；同一连接真实访问内容还要看证书、DNS、IP、HTTP 行为；ECH 普及后只能看到 outer SNI 或公共名，真实域名不可见。",
      s: "SNI 很好用，但把它当铁证就是安全产品里常见的误判来源。",
      lv: "intermediate"
    },
    {
      q: "JA3 指纹由哪些字段构成？它适合识别什么，不适合识别什么？",
      a: "JA3 通常基于 TLS 版本、cipher suites、extensions、elliptic curves 和 point formats 生成客户端指纹。它适合识别 TLS 客户端实现或恶意工具家族，不适合单独判定用户身份或业务域名。",
      d: "JA3 的价值在于很多客户端库会形成稳定的握手参数组合，例如某类恶意程序、扫描器或特定 SDK。但浏览器升级、GREASE、扩展顺序随机化、中间代理和库配置变化都会改变指纹。<br>实际分类要把 JA3 与 SNI、证书、ALPN、目标 IP、DNS、包长序列和行为统计一起使用，避免单特征过拟合。",
      s: "JA3 更像 TLS 世界里的 User-Agent 指纹，不是身份证。",
      lv: "intermediate"
    },
    {
      q: "不解密 TLS 的情况下，DPI 还能提取哪些行为特征做流量分类？",
      a: "可以提取 SNI、JA3/JA4、ALPN、证书元数据、DNS 关联、目标 ASN/CDN、包长序列、方向字节比、突发间隔、连接时长、并发连接数和重试行为。",
      d: "加密后 payload 不可见，但流量形态仍然泄露很多统计特征。视频、IM、远控、扫描器、VPN 隧道在包长、方向比例、连接频率和生命周期上差别很大。工程上常把规则特征和模型特征结合：规则负责高置信命中，模型负责模糊分类。<br>要注意隐私和合规边界，元数据虽然不是明文内容，也可能属于敏感数据。",
      s: "加密流量分类不是把锁撬开，而是看门口脚印、车牌和出入节奏。",
      lv: "advanced"
    },
    {
      q: "TLS 解密代理相比元数据识别有什么优势和代价？",
      a: "优势是能看到完整 HTTP 内容、URL、Header 和文件载荷，检测精度高；代价是证书部署复杂、隐私合规压力大、性能成本高，并且证书固定、双向 TLS、ECH/QUIC 都可能绕开或阻断解密。",
      d: "企业网关做 TLS inspection 通常要给终端下发根证书，在网关处建立两段 TLS。这样可以做 DLP、恶意文件检测和细粒度 URL 策略。<br>但这会破坏端到端安全假设，必须处理证书透明度、应用 pinning、金融/医疗白名单、性能加速和审计留痕。面试时不能只说'能解密就最好'，要能讲清边界和例外。",
      s: "TLS 解密是高权限手术刀，用不好就是隐私事故和兼容性事故。",
      lv: "advanced"
    },
    {
      q: "QUIC/HTTP3 为什么会让传统 TCP DPI 和流控策略失效？",
      a: "QUIC 基于 UDP，把传输可靠性、拥塞控制和 TLS 1.3 集成在用户态协议里。传统依赖 TCP 状态、明文 TLS ClientHello 或 HTTP/2 帧的解析逻辑会失去很多抓手。",
      d: "QUIC 的连接由 Connection ID 辅助迁移，不再只依赖四元组；握手和应用数据加密更深；多路复用不受 TCP 队头阻塞影响。网关若只把 UDP 当短流，会误删长连接；若只看 TCP 443，会漏掉 UDP 443 上的大量 HTTP/3。<br>分类上要解析 QUIC Initial、版本、DCID/SCID、SNI 可见性和流量行为，并准备面对 ECH 后元数据更少的场景。",
      s: "QUIC 不是'UDP 上的 HTTP'那么简单，它把很多过去在内核 TCP 里的状态搬到了加密用户态。",
      lv: "advanced"
    },
    {
      q: "epoll 的 LT 和 ET 有什么区别？为什么 ET 模式必须配合非阻塞 fd 读到 EAGAIN？",
      a: "LT 是水平触发，只要 fd 仍可读写就持续通知；ET 是边沿触发，只在状态从不可读变为可读等边沿变化时通知。ET 若没有一次性读到 EAGAIN，剩余数据可能再也没有事件唤醒。",
      d: "高性能 C++ 网关常用 epoll + nonblocking socket。ET 能减少重复通知，但要求事件处理循环不断 read/write，直到返回 EAGAIN 或 EWOULDBLOCK。否则缓冲区里还剩数据，状态没有新边沿，连接就会卡死。<br>写方向也一样：send 返回 EAGAIN 后再关注 EPOLLOUT，有空间后继续发送，不能假设一次写完。",
      s: "ET 模式是高效但苛刻的门铃，响一次你就必须把门口所有快递搬空。",
      lv: "intermediate"
    },
    {
      q: "高性能网关为什么常按 flow hash 分发到 worker，而不是让多个线程抢同一个连接？",
      a: "同一 flow 固定到同一 worker 能保持包序，避免 TCP 重组状态加锁，并提升 cache locality。多个线程抢同一 flow 会引入乱序、锁竞争和状态一致性问题。",
      d: "典型模型是 RSS/软件 hash 把五元组映射到队列或 worker，每个 worker 维护自己的流表分片和定时器。这样 packet path 上几乎不需要跨核共享状态。<br>跨 worker 迁移 flow 虽能做负载均衡，但要搬迁 TCP 重组缓存、DPI 解析上下文、NAT 映射和统计，代价很高。除非有明确热点流治理策略，否则先保证 flow affinity。",
      s: "网关多线程的第一原则不是线程越多越快，而是同一条流别被不同核来回扯。",
      lv: "advanced"
    },
    {
      q: "锁优化在流表设计里有哪些常见手段？",
      a: "常见手段包括分片锁、per-core 表、RCU 读多写少保护、批量更新、无锁 ring、原子计数、cache line 对齐和避免共享热点计数器。",
      d: "全局大锁会把多核网关退化成单核。读路径最好做到无锁或只读本核数据；写路径用分片降低冲突；统计计数优先 per-core 聚合，定期汇总。<br>还要防伪共享：两个高频计数器落在同一 cache line，会被 MESI 一直互相失效。使用 alignas(64) 或结构填充能显著降低 cache ping-pong。",
      s: "网络包路径每包都走，里面多一把热锁，吞吐就可能少一截。",
      lv: "advanced"
    },
    {
      q: "Linux 收包路径里 NAPI、RSS、RPS、GRO 分别解决什么问题？",
      a: "NAPI 用轮询减轻中断风暴；RSS 用网卡多队列把流分到不同 CPU；RPS 在软件层继续分发协议处理 CPU；GRO 把相邻小包合并成大包降低协议栈开销。",
      d: "高速网卡如果每个包都触发中断，CPU 会被中断压垮。NAPI 在高流量时转为 poll 批处理。RSS 通常用五元组 hash 选择 RX queue，使同一流落到同一队列。RPS 适合硬件队列不足或想按软件策略分发。GRO 提升吞吐但会改变抓包看到的包形态，排障时常需要关闭 offload 对照。",
      s: "懂这些机制，才知道性能瓶颈到底在网卡队列、软中断、协议栈还是应用线程。",
      lv: "intermediate"
    },
    {
      q: "sk_buff 在 Linux 网络栈里承担什么角色？为什么高速转发会嫌它重？",
      a: "sk_buff 是 Linux 内核中描述网络包的核心元数据结构，携带数据指针、协议头位置、校验和、路由、设备等上下文。它功能强但字段多、分配释放成本高。",
      d: "通用内核网络栈要支持各种协议、offload、netfilter、qdisc、tunnel 和 socket 语义，所以 skb 很强大。高速转发场景只需要少量字段时，skb 的内存访问和分配开销就显得重。<br>XDP、DPDK、netmap、AF_XDP 等方案的共同方向，是更早、更轻或直接在用户态处理 packet buffer，减少通用协议栈开销。",
      s: "skb 是全能工具箱，高速网关有时只想要一把小刀。",
      lv: "intermediate"
    },
    {
      q: "XDP/eBPF、TC、iptables/nftables 这些 hook 位置怎么选？",
      a: "XDP 最早，在驱动收包处适合丢包、限速、重定向等轻逻辑；TC 位于 qdisc 附近，适合更丰富的流量控制；iptables/nftables/conntrack 更靠后，适合状态防火墙、NAT 和复杂策略。",
      d: "越早处理，单位包成本越低，但可用上下文越少。XDP 还没创建 skb，速度快但不适合复杂 L7 解析；Netfilter 能用 conntrack 和 NAT，但已经走过更多内核路径。<br>产品架构常把粗粒度 DDoS 丢弃放 XDP，把状态策略放 nftables/自研 flow table，把深度应用识别放用户态 DPI。",
      s: "hook 位置就是性能和上下文的交易：越早越快，越晚越懂。",
      lv: "advanced"
    },
    {
      q: "DPDK 为什么能显著提升包处理性能？它付出了哪些代价？",
      a: "DPDK 通过用户态轮询驱动、hugepage、mbuf/mempool、批处理和绕过内核协议栈减少中断、拷贝和系统调用开销。代价是独占 CPU、绕开内核生态、驱动/NUMA/绑核配置复杂。",
      d: "DPDK poll mode driver 不等中断，而是在专用 core 上批量轮询 RX/TX ring；packet buffer 来自预分配 mempool，减少动态分配；hugepage 降低 TLB 压力。<br>代价也很现实：CPU 忙轮询耗电，普通 tcpdump/iptables 看不到用户态快路径，运维要处理 NIC 绑定、hugepage、NUMA 亲和性和多进程共享。",
      s: "DPDK 是把包处理从内核高速路拉到自建专用赛道，快，但所有红绿灯都得自己管。",
      lv: "advanced"
    },
    {
      q: "AF_XDP 和 DPDK 的定位有什么差异？",
      a: "AF_XDP 是 Linux XDP 体系里的高性能 socket，让 XDP 程序把包重定向到用户态 UMEM ring；DPDK 是更完整的用户态数据平面框架，通常直接接管网卡队列。",
      d: "AF_XDP 保留了更多 Linux 生态连接，可以按流量选择性旁路，适合渐进式加速；支持 zero-copy 时性能更接近 DPDK。DPDK 生态成熟，提供丰富库和 PMD，但部署侵入性更强。<br>选择时看需求：是否要和内核栈共存、NIC/驱动支持、是否需要现成 LPM/ACL/ring/timer 库、团队能否承担运维复杂度。",
      s: "AF_XDP 更像给 Linux 栈开一条快车道，DPDK 更像另建一套数据平面。",
      lv: "advanced"
    },
    {
      q: "SD-WAN 里的 underlay 和 overlay 分别指什么？",
      a: "underlay 是真实承载网络，如 MPLS、Internet、LTE；overlay 是在其上建立的逻辑网络，常由 IPsec/GRE/VXLAN 等隧道和控制平面组成，用统一策略连接分支、数据中心和云。",
      d: "underlay 负责基础可达性和物理链路质量，overlay 负责业务路由、加密、分段、策略和多路径调度。SD-WAN 的价值在于把多条质量不同的 underlay 链路抽象成可策略控制的 overlay。<br>排障时要分层：underlay 不通，隧道建不起来；overlay 控制平面异常，路由和策略下不去；数据平面异常，BFD/探测会显示丢包、时延或抖动异常。",
      s: "underlay 是公路，overlay 是车队自己的封闭路线和调度系统。",
      lv: "basic"
    },
    {
      q: "SD-WAN 如何根据链路质量做动态选路？常用探测指标有哪些？",
      a: "通过持续探测每条隧道的可用性、时延、抖动、丢包和带宽，结合业务 SLA 和策略选择出口；质量变差时切换到更合适路径。",
      d: "实时语音更怕 jitter 和 loss，文件同步更看重带宽和成本，关键业务可能要求固定走低丢包链路。控制器或边缘节点会把探测结果转成路径分数，并考虑负载、策略、地域和故障域。<br>切换不能过于敏感，否则轻微抖动会造成路径震荡；通常需要滑动窗口、迟滞阈值和最小保持时间。",
      s: "SD-WAN 不是简单多出口负载均衡，而是把业务 SLA 翻译成路径选择。",
      lv: "intermediate"
    },
    {
      q: "IPsec 隧道为什么经常引发 MTU/MSS 问题？怎么排查和修复？",
      a: "IPsec/隧道封装会增加额外头部，原本能通过的 1500 字节包可能超过路径 MTU，导致分片或被丢弃。常用修复是 PMTUD、MSS clamping、降低 tunnel MTU 和放行 ICMP Fragmentation Needed。",
      d: "ESP、UDP 封装、外层 IP、NAT-T 都会吃掉几十字节。若路径中丢弃 ICMP，又设置 DF 位，就会出现 PMTU black hole：小包正常，大包卡住。<br>排查时用 ping 设置 DF 和不同大小探测，结合 tcpdump 看是否有 ICMP too big。对 TCP 通常在 SYN 阶段改小 MSS；对 UDP 隧道则需要应用层分片或降低整体 MTU。",
      s: "VPN 能连上但业务偶发卡死，很多时候不是加密错了，而是包太胖过不了路。",
      lv: "intermediate"
    },
    {
      q: "OpenWrt 里 netifd、ubus、procd、UCI 分别是什么角色？",
      a: "UCI 是统一配置格式；netifd 管网络接口和协议状态；ubus 是系统内 IPC/RPC 总线；procd 是 init 与服务管理进程，负责拉起服务、监控和热插拔协作。",
      d: "做网关固件开发时，不能只会改配置文件。网络配置变更通常进入 UCI，netifd 解析后调用协议 handler 并监听 netlink 事件；服务间通过 ubus 查询和通知；procd 管服务生命周期和重启策略。<br>理解这套机制，才能做接口热更新、链路探测、VPN daemon 管理和故障自恢复，而不是每次变更都粗暴 reboot。",
      s: "OpenWrt 是小系统里的完整操作系统，netifd/ubus/procd 是它网络产品化的骨架。",
      lv: "basic"
    },
    {
      q: "网关固件升级为什么常用 A/B 分区和回滚机制？",
      a: "因为网关通常远程部署，升级失败会直接断网。A/B 分区让新固件写到备用分区，启动成功并健康检查通过后再确认；失败则自动回滚到旧分区。",
      d: "安全升级不仅是刷镜像。还要校验签名、防止断电写坏、保护配置迁移、记录启动次数、设置 watchdog 和 health check。对于 SD-WAN 设备，升级过程还要考虑隧道恢复、控制器重连和业务低峰窗口。<br>没有回滚机制的网关升级，本质是在远程无人机上直接换发动机。",
      s: "固件开发面试问升级机制，是在问你有没有产品现场故障意识。",
      lv: "intermediate"
    },
    {
      q: "用 Wireshark 分析 TCP 乱序、重传和丢包时，为什么必须关心抓包点？",
      a: "因为抓包点决定你看到的是发送前、发送后、接收前还是接收后视角。单点抓包无法直接证明包在网络中哪里丢，也可能受本机 offload 影响看到异常大包或校验和假错。",
      d: "发送端抓到包不代表对端收到；接收端没抓到不代表发送端没发。中间设备、镜像口丢包、虚拟交换机和网卡 offload 都会改变证据。严谨排障通常需要双端同步抓包，按时间戳、seq/ack 和五元组对齐。<br>Wireshark 的 out-of-order/retransmission 标记是分析器推断，不是协议头里的字段。",
      s: "抓包不是截图留念，而是带坐标的证据采集；坐标错了，结论就会歪。",
      lv: "intermediate"
    },
    {
      q: "IDS、IPS、防火墙和 DPI 的职责边界有什么区别？",
      a: "防火墙侧重访问控制和状态策略；IDS 侧重检测和告警；IPS 在线阻断可疑流量；DPI 是深度解析流量内容或协议元数据的技术能力，可被防火墙、IDS、IPS 共同使用。",
      d: "传统防火墙看五元组、状态和策略；下一代防火墙会加入应用识别、用户和内容安全。IDS 通常旁路部署，误报影响较小但无法直接阻断；IPS 在线串接，能阻断但必须极其关注误杀和性能。<br>DPI 不是一个产品名，而是一套解析和特征提取能力：从 HTTP URL、TLS 指纹到工业协议字段都可能属于 DPI 范围。",
      s: "面试官让你区分这些词，是在看你能否从产品形态、部署位置和风险代价三个层面理解安全网关。",
      lv: "basic"
    },
    {
      q: "DPI 规则引擎如何在高吞吐下匹配大量特征？",
      a: "通常会使用多级过滤：先用端口、协议、方向、长度等便宜条件缩小范围，再用 Trie、Aho-Corasick、Hyperscan、位图或哈希匹配复杂字符串和签名。",
      d: "不能每个包都遍历全部规则。工程实现会把规则编译成自动机或索引结构，并按协议解析阶段触发。例如 TLS 只在 ClientHello 完整后跑 SNI/JA3 规则，HTTP 只在 Header 完整后匹配 Host/URI。<br>还要处理跨包匹配、大小写归一化、编码解码、压缩、流式扫描和规则热更新。性能优化往往来自减少候选规则数量，而不是盲目手写汇编。",
      s: "DPI 快不快，关键看你能不能把十万条规则变成几次有方向的查表。",
      lv: "advanced"
    },
    {
      q: "高速包处理里的 ring buffer 和 backpressure 应该怎么设计？",
      a: "ring buffer 用固定大小循环队列降低分配开销；backpressure 需要在队列接近满时限速、丢弃低优先级流、上报码流压力或触发采样降级，不能让内存无限增长。",
      d: "生产网关里 RX、DPI、日志、上报、存储之间速度不可能永远一致。无界队列会把瞬时流量峰值变成内存雪崩；有界 ring 能把问题显式化。<br>丢弃策略要和业务目标一致：安全告警可以采样，控制报文要优先，普通大流可以降级只保留元数据。还要暴露 dropped、queue depth、latency 等指标，否则 backpressure 只是在黑盒里悄悄丢包。",
      s: "高性能系统不是永不堵车，而是堵车时知道在哪排队、谁先过、谁可以丢。",
      lv: "advanced"
    },
    {
      q: "为什么网关产品常把控制面和数据面拆开？",
      a: "数据面负责每包高速转发和轻量策略执行，控制面负责配置、路由、证书、策略下发、统计聚合和运维管理。拆开后可以让快路径稳定低延迟，同时避免复杂管理逻辑拖慢转发。",
      d: "SD-WAN、DPDK 网关和防火墙都常见这种结构。控制面变更策略时不能直接阻塞数据面，通常使用版本化规则、RCU、双缓冲或增量下发；数据面上报统计也要批量异步，避免每包 RPC。<br>控制面崩溃时，数据面是否继续按最后策略转发，是产品可靠性设计的重要问题。",
      s: "控制面像调度室，数据面像收费站通道；调度室开会不能让所有车停在闸口等。",
      lv: "intermediate"
    },
    {
      q: "如何设计一个可观测的 TCP/UDP 流表模块？关键指标有哪些？",
      a: "需要暴露流表容量、命中率、创建/删除速率、各状态数量、超时回收数、淘汰数、冲突链长度、锁等待、每核负载、重组缓存占用和异常包计数。",
      d: "没有指标的流表很难排障。流表满时要知道是 SYN 半连接暴涨、UDP idle timeout 太长、长连接泄漏，还是 hash 分布不均。重组模块还要看 out-of-order 队列、overlap、gap timeout 和内存上限命中。<br>指标最好按 zone/租户/接口/worker 维度拆分，并配合少量采样日志输出具体五元组，避免只有全局总数看不出根因。",
      s: "能转发只是第一步，能解释为什么转发变慢才是工程化。",
      lv: "intermediate"
    }
  ]
};
